export class Designation {
  code: string;
  couleur: string;
  icon: string;

  constructor(code: string, couleur: string, icon: string) {
    this.code = code;
    this.couleur = couleur;
    this.icon = icon;
  }
}
